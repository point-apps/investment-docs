export const description =
  'On this page, we’ll dive into the different owner endpoints you can use to manage owners programmatically.'

# Owners

## The owner model

The owner model contains all the information about your owners.

### Properties

<Properties>
  <Property name="_id" type="objectId">
    Unique identifier for the owner.
  </Property>
  <Property name="name" type="string">
    The name for the owner.
    - Unique Constraint
    - Is Required
  </Property>
  <Property name="createdBy_id" type="objectId">
    User identifier who create the owner.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the owner was created.
  </Property>
  <Property name="updatedBy_id" type="objectId">
    User identifier who update the owner.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the owner was updated.
  </Property>
  <Property name="archivedBy_id" type="objectId">
    User identifier who archive the owner.
  </Property>
  <Property name="archivedAt" type="timestamp">
    Timestamp of when the owner was archived.
  </Property>
  <Property name="requestApprovalDeleteTo_id" type="objectId">
    User identifier who request to delete the owner.
  </Property>
  <Property name="requestApprovalDeleteAt" type="timestamp">
    Timestamp of when the user request to delete the owner.
  </Property>
  <Property name="requestApprovalDeleteReason" type="string">
    Request approval delete reason.
  </Property>
  <Property name="requestApprovalDeleteReasonReject" type="string">
    Reason to reject request delete.
  </Property>
  <Property name="requestApprovalDeleteStatus" type="enum">
    Request approval delete status
    - enum ['pending', 'approved', 'rejected']
  </Property>
</Properties>

---

## List all owners {{ tag: 'GET', label: '/v1/owners' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your owners.

    ### Optional query

    <Properties>
      <Property name="page" type="integer">
        The page number of owners returned.
        - Default `1`
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of owners returned.
        - Default `10`
      </Property>
      <Property name="search" type="object">
        Filter data by text
      </Property>
      <Property name="sort" type="object">
        Display data in order.
        - `asc` sort ascending
        - `desc` sort descending
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/owners">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/owners', {
      params: {
        page:1,
        pageSize: 10,
        search: {
          name: 'john',
        },
        sort: {
          name: 'asc'
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "owners": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "John Doe",
          "createdBy_id": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Jane",
          "createdBy_id": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create the owner {{ tag: 'POST', label: '/v1/owners' }}

<Row>
  <Col>

    This endpoint allows you to add a new owner.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the owner.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/owners">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/owners', {
      name: "Charles",
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "_id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve the owner {{ tag: 'GET', label: '/v1/owners/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve the owner by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/owners/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/owners/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Charles",
      "createdBy_id": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update the owner {{ tag: 'PATCH', label: '/v1/owners/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on the owner.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the owner.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf', {
      name: "Administrator",
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete the owner {{ tag: 'DELETE', label: '/v1/owners/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/owners/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/owners/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Archive the owner {{ tag: 'PATCH', label: '/v1/owners/:id/archive' }}

<Row>
  <Col>
    This endpoint allows you to patch owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id/archive">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf/archive')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Restore the owner {{ tag: 'PATCH', label: '/v1/owners/:id/restore' }}

<Row>
  <Col>
    This endpoint allows you to patch owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id/restore">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf/restore')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Request to delete the owner {{ tag: 'PATCH', label: '/v1/owners/:id/request-delete' }}

<Row>
  <Col>
    This endpoint allows you to request delete owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id/request-delete">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf/request-delete', {
      approvalTo: '',
      reasonDelete: '',
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Approve request to delete the owner {{ tag: 'PATCH', label: '/v1/owners/:id/request-delete/approve' }}

<Row>
  <Col>
    This endpoint allows you to delete owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf/request-delete/approve')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Reject request to delete the owner {{ tag: 'PATCH', label: '/v1/owners/:id/request-delete/reject' }}

<Row>
  <Col>
    This endpoint allows you to delete owners from your owner list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/owners/:id/request-delete/reject">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/owners/637d83d15d2be122007524bf/request-delete/reject', {
      reasonReject: ""
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---